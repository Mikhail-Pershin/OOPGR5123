public class FileLogger implements CalculationObserver {
    public void onCalculation(ComplexNumber num1, ComplexNumber num2, ComplexOperation operation, ComplexNumber result) {
        // Логика записи в файл
    }
}